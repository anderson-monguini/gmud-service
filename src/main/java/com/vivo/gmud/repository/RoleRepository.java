package com.vivo.gmud.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.vivo.gmud.model.Role;

public interface RoleRepository extends JpaRepository<Role, Long>{
}
